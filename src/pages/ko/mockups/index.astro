---
import MockupsPage from "@/components/pages/MockupsPage.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getDictionary } from "@/i18n/messages";

const locale = "ko";
const dictionary = getDictionary(locale);
const title = `${dictionary.meta.siteName} Mockups | ${dictionary.mockupsPage.eyebrow}`;
const description = dictionary.mockupsPage.description;
---

<BaseLayout locale={locale} title={title} description={description}>
  <MockupsPage locale={locale} dictionary={dictionary} />
</BaseLayout>
